import { FormQuantityPipe } from './form-quantity.pipe';

describe('FormQuantityPipe', () => {
  it('create an instance', () => {
    const pipe = new FormQuantityPipe();
    expect(pipe).toBeTruthy();
  });
});
